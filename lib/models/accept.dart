import 'package:flutter/material.dart';

class AcceptPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Accept Page'),
      ),
      body: Center(
        child: Text('This is the Accept Page'),
      ),
    );
  }
}
